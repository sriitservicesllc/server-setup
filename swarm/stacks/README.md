# Swarm stacks

These files are the Git-tracked stack definitions that SwarmCD can watch.

The Ansible role also renders equivalent files into `/opt/swarm-platform/rendered` on the first manager so the cluster can be bootstrapped before GitOps takes over.
